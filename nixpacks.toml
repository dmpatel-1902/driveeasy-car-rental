providers = ["python"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
    "python manage.py collectstatic --noinput",
    "python manage.py migrate --run-syncdb"
]

[start]
cmd = "gunicorn car_rental.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
